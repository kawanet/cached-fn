{
  "name": "cached-fn",
  "description": "Lightweight and lazy cached function with TTL, negative TTL, and item limit controls.",
  "version": "1.2.0",
  "author": "@kawanet",
  "bugs": {
    "url": "https://github.com/kawanet/cached-fn/issues"
  },
  "contributors": [
    "kawanet <u-suke@kawa.net>"
  ],
  "devDependencies": {
    "@rollup/plugin-alias": "^6.0.0",
    "@rollup/plugin-multi-entry": "^7.0.0",
    "@rollup/plugin-node-resolve": "^16.0.2",
    "@rollup/plugin-sucrase": "^5.0.2",
    "@rollup/plugin-terser": "^1.0.0",
    "@types/node": "^24.7.2",
    "rollup": "^4.52.4",
    "terser": "^5.44.0",
    "typescript": "^5.9.3"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": ">=22",
      "onFail": "warn"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./types/cached-fn.d.ts",
      "require": "./dist/cached-fn.cjs",
      "import": "./dist/cached-fn.mjs"
    }
  },
  "files": [
    "LICENSE",
    "README.md",
    "dist/cached-fn.cjs",
    "dist/cached-fn.min.js",
    "dist/cached-fn.mjs",
    "dist/package.json",
    "src/*.ts",
    "types/*.d.ts"
  ],
  "homepage": "https://github.com/kawanet/cached-fn",
  "keywords": [
    "cache",
    "caching",
    "lazy",
    "memoize"
  ],
  "license": "MIT",
  "main": "./dist/cached-fn.cjs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kawanet/cached-fn.git"
  },
  "scripts": {
    "build": "make -C builder",
    "fixpack": "fixpack",
    "prepack": "if node -v | grep -q '^v20'; then echo 'ERROR: packing is not allowed on Node 20'; exit 1; fi",
    "prepare": "if node -v | grep -q '^v20'; then :; else make -C builder clean all test; fi",
    "test": "make -C builder test"
  },
  "sideEffects": false,
  "type": "module",
  "types": "./types/cached-fn.d.ts"
}
